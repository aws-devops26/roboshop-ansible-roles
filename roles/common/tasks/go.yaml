- name: install go
  ansible.builtin.dnf:
    name: golang
    state: present

- name: build go application
  ansible.bultin.command: "{{item}}"
  loop:
  - cd /app 
  - go mod init dispatch
  - go get 
  - go build
  args:
    chdir: /app